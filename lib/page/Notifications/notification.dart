import 'package:flutter/material.dart';
class SchoolNotification extends StatelessWidget {
  const SchoolNotification({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:Text("Notification")),
      
    );
  }
}