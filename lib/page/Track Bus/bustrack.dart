import 'package:flutter/material.dart';
class TrackBus extends StatelessWidget {
  const TrackBus({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Track Bus")),
      
    );
  }
}