import 'package:flutter/material.dart';
class SchoolLibrary extends StatelessWidget {
  const SchoolLibrary({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
      appBar: AppBar(title:Text("Library")),
      
    );
  }
}