import 'package:flutter/material.dart';
class SchoolFees extends StatelessWidget {
  const SchoolFees({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:Text("Fees ")),
      
    );
  }
}