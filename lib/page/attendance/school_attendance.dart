import 'package:flutter/material.dart';
class SchholAttendance extends StatelessWidget {
  const SchholAttendance({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:Text("Attendance")),
      
    );
  }
}