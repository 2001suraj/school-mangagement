import 'package:flutter/material.dart';
class SchoolApplyLeave extends StatelessWidget {
  const SchoolApplyLeave({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:Text("Apply leave")),
    );
  }
}