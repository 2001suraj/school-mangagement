import 'package:flutter/material.dart';
class SchoolLogout extends StatelessWidget {
  const SchoolLogout({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:Text(" Logout")),
      
    );
  }
}