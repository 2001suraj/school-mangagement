import 'package:flutter/material.dart';
class SchoolExamPage extends StatelessWidget {
  const SchoolExamPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:AppBar(title:Text("Exam "))
      
    );
  }
}