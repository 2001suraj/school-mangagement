import 'package:flutter/material.dart';
class SchoolStudentProfile extends StatelessWidget {
  const SchoolStudentProfile({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:Text("Profile")),
      
    );
  }
}